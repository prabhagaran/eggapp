# Domain layer

Pure domain logic (batch lifecycle guards BR-001/BR-008, ledger conservation
BR-009, metric definitions BR-015). No Fastify, no Prisma, no MQTT imports —
dependencies point inward only.
