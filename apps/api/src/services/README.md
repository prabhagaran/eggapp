# Application services

Use-cases live here. Auth/RBAC and farm scoping (BR-013) are enforced in
this layer — not in route handlers, not in the MQTT adapter
(backend-architect Definition of Done; layering per
`docs/architecture/system-architecture.md`).
