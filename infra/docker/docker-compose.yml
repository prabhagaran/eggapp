# Local/production broker for the personal deployment (ADR 0004).
# Requires Docker Desktop. First run: create the password file per
# mosquitto/README.md, then: docker compose up -d
services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: eggapp-mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./mosquitto/passwd:/mosquitto/config/passwd:ro
      - mosquitto-data:/mosquitto/data

  # apps/api runs natively via systemd on this same host, not in Docker —
  # see ADR 0007 and infra/systemd/eggapp-api.service. Reconnects to this
  # broker over localhost:1883.

volumes:
  mosquitto-data:
