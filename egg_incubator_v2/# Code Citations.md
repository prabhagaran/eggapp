# Code Citations

## License: unknown
https://github.com/jeromesun14/blog/blob/09f97b5678db99a565d20f319a67fe1c89ad4788/_posts/programmer/tools/kconfig-frontends.md

```
Good — I have the full current script. Since no file-editing tools are available, here is the complete replacement content for `google_apps_script.txt`. Copy and paste it entirely over the current file:

```javascript
/**
 * Apps Script: Telemetry receiver for ESP32 Egg Incubator firmware.
 *
 * INSTRUCTIONS:
 *  1. Replace SHEET_ID below with your Google Sheet ID
 *     (the long string in the Sheet URL between /d/ and /edit).
 *  2. Set TOKEN to match CLOUD_TOKEN in your firmware's config.h / secrets.h.
 *  3. Save, then:
 *       Deploy → New Deployment → Web App
 *       Execute as: Me
 *       Who has access: Anyone
 *  4. Copy the deployment URL into GOOGLE_SCRIPT_URL in secrets.h.
 *
 * Two sheets are auto-created on first use:
 *   Telemetry  — all normal sensor readings
 *   ErrorLogs  — error events (when `error` param is present and non-empty)
 *
 * Accepts both HTTP GET and HTTP POST (JSON body or URL parameters).
 * Returns plain-text "OK" (HTTP 200) on success.
 */

// ─── Configuration ────────────────────────────────────────────────────────────
const TOKEN    = 'a1b2c3d4e5f60718293a4b5
```


## License: unknown
https://github.com/jeromesun14/blog/blob/09f97b5678db99a565d20f319a67fe1c89ad4788/_posts/programmer/tools/kconfig-frontends.md

```
Good — I have the full current script. Since no file-editing tools are available, here is the complete replacement content for `google_apps_script.txt`. Copy and paste it entirely over the current file:

```javascript
/**
 * Apps Script: Telemetry receiver for ESP32 Egg Incubator firmware.
 *
 * INSTRUCTIONS:
 *  1. Replace SHEET_ID below with your Google Sheet ID
 *     (the long string in the Sheet URL between /d/ and /edit).
 *  2. Set TOKEN to match CLOUD_TOKEN in your firmware's config.h / secrets.h.
 *  3. Save, then:
 *       Deploy → New Deployment → Web App
 *       Execute as: Me
 *       Who has access: Anyone
 *  4. Copy the deployment URL into GOOGLE_SCRIPT_URL in secrets.h.
 *
 * Two sheets are auto-created on first use:
 *   Telemetry  — all normal sensor readings
 *   ErrorLogs  — error events (when `error` param is present and non-empty)
 *
 * Accepts both HTTP GET and HTTP POST (JSON body or URL parameters).
 * Returns plain-text "OK" (HTTP 200) on success.
 */

// ─── Configuration ────────────────────────────────────────────────────────────
const TOKEN    = 'a1b2c3d4e5f60718293a4b5
```


## License: unknown
https://github.com/jeromesun14/blog/blob/09f97b5678db99a565d20f319a67fe1c89ad4788/_posts/programmer/tools/kconfig-frontends.md

```
Good — I have the full current script. Since no file-editing tools are available, here is the complete replacement content for `google_apps_script.txt`. Copy and paste it entirely over the current file:

```javascript
/**
 * Apps Script: Telemetry receiver for ESP32 Egg Incubator firmware.
 *
 * INSTRUCTIONS:
 *  1. Replace SHEET_ID below with your Google Sheet ID
 *     (the long string in the Sheet URL between /d/ and /edit).
 *  2. Set TOKEN to match CLOUD_TOKEN in your firmware's config.h / secrets.h.
 *  3. Save, then:
 *       Deploy → New Deployment → Web App
 *       Execute as: Me
 *       Who has access: Anyone
 *  4. Copy the deployment URL into GOOGLE_SCRIPT_URL in secrets.h.
 *
 * Two sheets are auto-created on first use:
 *   Telemetry  — all normal sensor readings
 *   ErrorLogs  — error events (when `error` param is present and non-empty)
 *
 * Accepts both HTTP GET and HTTP POST (JSON body or URL parameters).
 * Returns plain-text "OK" (HTTP 200) on success.
 */

// ─── Configuration ────────────────────────────────────────────────────────────
const TOKEN    = 'a1b2c3d4e5f60718293a4b5
```


## License: unknown
https://github.com/jeromesun14/blog/blob/09f97b5678db99a565d20f319a67fe1c89ad4788/_posts/programmer/tools/kconfig-frontends.md

```
Good — I have the full current script. Since no file-editing tools are available, here is the complete replacement content for `google_apps_script.txt`. Copy and paste it entirely over the current file:

```javascript
/**
 * Apps Script: Telemetry receiver for ESP32 Egg Incubator firmware.
 *
 * INSTRUCTIONS:
 *  1. Replace SHEET_ID below with your Google Sheet ID
 *     (the long string in the Sheet URL between /d/ and /edit).
 *  2. Set TOKEN to match CLOUD_TOKEN in your firmware's config.h / secrets.h.
 *  3. Save, then:
 *       Deploy → New Deployment → Web App
 *       Execute as: Me
 *       Who has access: Anyone
 *  4. Copy the deployment URL into GOOGLE_SCRIPT_URL in secrets.h.
 *
 * Two sheets are auto-created on first use:
 *   Telemetry  — all normal sensor readings
 *   ErrorLogs  — error events (when `error` param is present and non-empty)
 *
 * Accepts both HTTP GET and HTTP POST (JSON body or URL parameters).
 * Returns plain-text "OK" (HTTP 200) on success.
 */

// ─── Configuration ────────────────────────────────────────────────────────────
const TOKEN    = 'a1b2c3d4e5f60718293a4b5
```

