# Changelog

All notable changes and fixes for the egg_incubator_v2 workspace.

## v2 — 2026-03-05 — Released

- Release `v2`: consolidated documentation, updated README, added per-FSM
  design pages, and included a host-run unit-test scaffold for the
  Temperature FSM. See entries below for detailed changes.



## 2026-03-05 — Documentation

- Formalized and clarified the repository `README.md` with a concise project
  overview, structured quick-start instructions, repository layout, and
  guidance for configuration and contribution.



## 2026-03-04 — Maintenance & fixes

- Fan / PWM (RELAY_FAN) conflict — applied FIX Option A
  - Problem: `RELAY_FAN` (GPIO 13) was used both for LEDC PWM and digital
    writes; `digitalWrite()` detached LEDC and broke PWM. `gRelayState.fanOn`
    was never updated by PWM path so telemetry reported `fan=0`.
  - Change: removed `RELAY_FAN` handling from `setRelay()` and replaced
    direct digital write in `allRelaysOff()` with a call to `setFanSpeed(0)`.
    `setFanSpeed()` now updates `gRelayState.fanOn` under `controlMutex`.
  - Files changed: [globals.h](globals.h), [globals.cpp](globals.cpp),
    [task_incubator.cpp](task_incubator.cpp)
  - Test: project compiled successfully; run on device and telemetry now
    reports fan state according to PWM value.

- Turner last-turn timestamp correction
  - Problem: `lastTurnEpoch` was saved using the epoch read before the turn
    action, causing the reported idle interval to be (interval - duration).
  - Change: after the turn completes, firmware now re-reads the RTC and
    persists the post-turn epoch as `lastTurn` in NVS.
  - Files changed: [task_incubator.cpp](task_incubator.cpp)
  - Test: code compiled; recommend running device and verifying turn timestamp
    reported on next telemetry/after reboot.

- Cloud secrets and deployment handling
  - Change: moved cloud endpoint and token to a local gitignored `secrets.h`;
    added `secrets.h.example` with placeholders and added `.gitignore` entry.
  - Also updated `config.h` to optionally `#include "secrets.h"` when present
    (safe empty defaults otherwise).
  - Files changed: [config.h](config.h), [secrets.h.example](secrets.h.example),
    [secrets.h](secrets.h), [.gitignore](.gitignore)
  - Test: compiled firmware; cloud telemetry continues to upload (HTTP 200)
    when `secrets.h` contains a valid URL + token.

### Notes / Next steps
- Security: populate `CLOUD_ROOT_CA` in `secrets.h` to enable TLS verification.
- Functional: follow-up fixes recommended in the code review (report.txt):
  - Re-check `lastTurnEpoch` timestamp update (should be read after turn)
  - Persist climate cycle/ramp state to NVS
  - Persist lockdown humidity change to NVS
  - Guard `task_pump()` with `hum_valid` before triggering

If you want, I can now:
- Insert a provided Root CA PEM into `secrets.h` and recompile.
- Open the serial monitor to observe telemetry/fan state live.

---
Generated by GitHub Copilot assistant while reviewing and updating the
egg_incubator_v2 project.

## 2026-03-05 — UI overhaul, sensor reliability, and watchdog fixes

- DHT11 reliability: switched to a single read per cycle with a safe
  2.5s sampling interval, added warm-up after `dht.begin()` and an automatic
  re-init every 5 consecutive failures to recover a stuck sensor bus.
  - Files changed: [task_sensors.cpp](task_sensors.cpp)

- Modern OLED menu UI: replaced `>` text cursor with a highlighted selection
  bar, text inversion for the selected row, and a right-side 3px vertical
  scrollbar. Menus now paginate with a 4-row visible window for smooth
  navigation and consistent layout.
  - Files changed: [oled_ui.cpp](oled_ui.cpp), [oled_ui.h](oled_ui.h)

- Menu layout tweak: highlight bar baseline adjusted so menu text starts at
  y=18 to avoid overlap with the OLED header band.
  - Files changed: [oled_ui.cpp](oled_ui.cpp)

- Pagination & navigation: climate and system menus now use a sliding
  window (4 visible rows) matching the new UI primitives; `task_ui.cpp`
  updated to track `climMenuTop` / `sysMenuTop` offsets.
  - Files changed: [task_ui.cpp](task_ui.cpp)

- Watchdog and runtime stability:
  - Fixed TWDT init to use `esp_task_wdt_config_t` (IDF v5 API).
  - Removed `esp_task_wdt_add(NULL)` / `esp_task_wdt_reset()` from tasks that
    may be suspended during profile switching to avoid watchdog-triggered
    resets. Keep `esp_task_wdt_init()` in `setup()` only.
  - Files changed: [egg_incubator_v2.ino](egg_incubator_v2.ino), [task_control.cpp](task_control.cpp), [task_climate_control.cpp](task_climate_control.cpp)

- Misc fixes completed during this session (NVS namespace, RTC/epoch
  sanity, mutex relocation, config cleanups). See previous entries for
  details and individual file changes.

---

Notes:
- Firmware compiled and uploaded successfully after these changes.
- If you want the changelog entry split into a formal release note
  (version bump / release tag), tell me the desired version string.
